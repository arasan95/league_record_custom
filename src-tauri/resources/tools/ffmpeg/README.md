# Bundled FFmpeg

Place `ffmpeg.exe` in this folder for bundled clip support.

Required files:
- `ffmpeg.exe`
- `ffmpeg-provenance.json` (already included template)

The app will prefer this bundled binary when `ffmpegPath` is not set.